# Public Technical Requirement Registry (curated v0.2)
# Curated public registry subset. IDs are permanent — never renumbered.
#
# System slugs: SEC, TEST, ADR, AGT, GOV, PUB (public release)

requirements:

  - id: TR-SEC-001
    title: No hardcoded secrets
    status: active
    section: "Secrets Management"
    text: >
      All secrets must be stored in a vault. Never hardcoded in configuration files,
      source code, comments, or test fixtures.

  - id: TR-SEC-002
    title: PII encrypted at rest
    status: active
    section: "Data Privacy"
    text: >
      Personally identifiable information must be encrypted at rest when stored
      by an application.

  - id: TR-SEC-003
    title: PII routed to local LLM only
    status: active
    section: "Agents"
    text: >
      For privacy-focused use cases, local LLMs must be used. Cloud-backed agents must not
      access private notes, secrets, private repos, or unrestricted home directories.

  - id: TR-SEC-005
    title: Externally retrieved content treated as untrusted
    status: active
    section: "Prompt Injection"
    text: >
      Retrieved content must not authorize tool calls or override system-level reasoning.
      When retrieved content may contain PII, TR-SEC-003 routing takes precedence.

  - id: TR-SEC-008
    title: Local credential files are permission-restricted and secret-scanned
    status: active
    section: "Secrets Management"
    text: >
      Files holding secrets on a development or production host (.env files, key files,
      token caches) must be readable by the owning user only (mode 600). While secrets
      live in local files rather than a vault (TR-SEC-001's target state), a secret-scanning
      check must run before commit, not only at public-release time. Counters MITRE ATT&CK
      T1552.001 (Unsecured Credentials: Credentials in Files).

  - id: TR-SEC-009
    title: CI pipelines run least-privilege and fully pinned
    status: active
    section: "CI/CD Supply Chain"
    text: >
      Every CI workflow declares an explicit least-privilege permissions block (default
      contents: read; broader scopes granted per-job with a comment stating why).
      Third-party CI actions are pinned to a full commit SHA, not a mutable tag, with the
      human-readable version in a trailing comment. Runtime dependencies past POC tier
      are pinned to exact versions so the audited dependency set is the set that runs.
      Counters MITRE ATT&CK T1195 (Supply Chain Compromise) and limits the blast radius
      of a compromised workflow token.

  - id: TR-SEC-010
    title: Agent tool permission grants are a security boundary (least agency)
    status: active
    section: "Agent Security"
    text: >
      Permission allowlists for coding agents (e.g. tool-grant or command-allowlist
      config) are security boundaries, not conveniences: a prompt-injected agent
      session (TR-SEC-005) can invoke any allowlisted command without human review.
      Wildcard grants for arbitrary file writes, package installation, unrestricted
      exec, or unrestricted network access must not be allowlisted; grant the specific
      command needed instead. This is OWASP's least-agency principle (least privilege
      extended to what an agent tool can do, how often, and where) applied to agent
      tooling. Review all agent permission grants at each governance review
      (TR-GOV-006). Counters MITRE ATT&CK T1059 (Command and Scripting Interpreter)
      and T1195.002 executed through trusted developer tooling.

  - id: TR-SEC-011
    title: Content provenance tracked and trust derived fail-closed at retrieval
    status: active
    section: "Prompt Injection"
    text: >
      Content ingested into an agentic memory or RAG index must be tagged with its
      source type at write time. Trust level is derived from source type via a
      mapping defined in code (not stored as data), so a mapping revision is a code
      change, not a migration. The mapping is fail-closed: only explicitly named
      self-authored source types earn the most-trusted tier; any unrecognized or
      unclassified type falls to the least-trusted tier, and content whose
      provenance was never recorded is treated as unverified, equivalent to
      untrusted. Provenance is validated at retrieval time, not only at ingest.
      Untrusted and unverified content is quarantined data under TR-SEC-005 —
      spotlighted at the reasoning boundary, never treated as instructions.

  - id: TR-SEC-012
    title: Strict LLM output-schema validation — reject, never coerce
    status: active
    section: "Prompt Injection"
    text: >
      Every field returned by a model call must pass both a type check and a
      range/shape check before use. A field that fails either check must be
      rejected, never silently coerced to a compatible type. Coercion can fail
      open: Python's bool("false") evaluates to True because any non-empty string
      is truthy, so a bare bool() cast on a model-returned string can flip a
      relevance or safety gate the wrong way. A missing optional field is a
      defined, valid state; a present field of the wrong type is not, and the two
      must not share a fallback path. Pairs with TR-GOV-001's single-source-of-truth
      convention — a strict parser for a given output schema is defined once and
      used by every caller of that boundary.

  - id: TR-SEC-013
    title: Two-layer isolation for multi-agent tool and data access
    status: active
    section: "Agent Security"
    text: >
      When multiple agents share one backing service (a tool surface and the
      data behind it), isolation must be enforced at two independent layers,
      not one. Layer one is the tool-registry/authorization scope — distinct
      credentials per agent, with the server deciding which tools each
      credential may even invoke; this bounds what is *offered* to a given
      agent's own reasoning. Layer two is the data-layer scope — per-agent
      database roles or equivalent, bounding what is *reachable* regardless
      of what the authorization layer believes it has granted. Neither layer
      is a substitute for the other: a bug in the tool-registry layer (a
      stray wildcard registration, a misrouted credential map) can hand an
      agent a tool it should never have received, and the data-layer role is
      what still blocks the resulting call; a data layer alone, with no
      tool-registry scope, would still let a compromised or over-broad tool
      call reach every table a shared credential can see. Extends TR-SEC-010's
      least-agency principle from single-agent tool grants to multi-agent
      tool *and* data boundaries. The most-exposed agent (the one with an
      external input path) gets the narrowest grant at both layers; the most
      broadly-privileged agent gets no external egress at all.

  - id: TR-SEC-014
    title: Credential-isolated broker operations are scope-bound and single-use
    status: active
    section: "Agent Security"
    text: >
      An untrusted or air-gapped agent must not receive credentials for an external system.
      A host-side broker may expose only named operations; each request must validate the exact
      resource scope and immutable event identity, bind mutation leases to that identity,
      revalidate immediately before mutation, and consume each lease exactly once. Broker
      transport mounts must survive broker restarts without granting the agent write access to
      the socket directory.

  - id: TR-SEC-015
    title: Graded outlet confidence never upgrades a TR-SEC-011 trust tier
    status: active
    section: "Prompt Injection"
    text: >
      Alongside the fail-closed trust tier TR-SEC-011 requires, a system may attach an
      optional, more finely graded confidence about the specific outlet a piece of
      untrusted content came from (e.g. a curated reference source vs. an
      unrecognized domain). This tier is informational only: it must never promote
      content to a more trusted tier, and it must never change how retrieved content
      is presented at the reasoning boundary — a highly graded outlet's content is
      still delimited and spotlighted exactly like an ungraded one. The grading source
      is a small, curated, reviewable allowlist; absence from it is the fail-closed
      default, never inferred as trustworthy. Downstream weighting or disclosure may
      read the grade; no trust or spotlighting decision may.

  - id: TR-TEST-001
    title: TDD required for core logic
    status: active
    section: "Testing"
    text: >
      Tests are written before or alongside core logic. Scaffolding may come
      first, but business logic should not be considered complete without tests.

  - id: TR-TEST-002
    title: Coverage targets by deployment tier
    status: active
    section: "Testing"
    text: >
      POC projects may use targeted coverage. Local Production targets at least
      70 percent coverage. Cloud Production targets at least 90 percent coverage.

  - id: TR-TEST-004
    title: LLM regression benchmark on model switch
    status: active
    section: "Testing"
    text: >
      When switching or upgrading a runtime LLM, run a standard task set against
      the old and new models and record cost, latency, correctness, and schema
      validation deltas. See templates/llm-regression-benchmark.md.

  - id: TR-ADR-001
    title: ADR required for significant design decisions
    status: active
    section: "Architecture Decision Records"
    text: >
      An ADR must be created for each significant design decision including agent
      framework choice and trigger classification (TR-AGT-004).

  - id: TR-AGT-003
    title: Agentic node loop contracts
    status: active
    section: "Agent Spawning"
    text: >
      Every agent pipeline node declares input schema, output schema, exit condition,
      and resource budget before implementation. Nodes exposed as MCP tools additionally
      declare four tool annotation hints (readOnlyHint, destructiveHint, idempotentHint,
      openWorldHint) describing blast radius — required only for MCP-exposed nodes.
      Annotations must be earned, not merely asserted: each hint names a property the
      implementation actually has, and the test suite must demonstrate it (e.g.
      idempotentHint requires a real no-op path). Declaring a hint without the property
      is a protocol violation — callers that retry on a false idempotentHint will amplify
      damage. A triage or gate node's output schema may additionally include a
      disposition field drawn from a closed vocabulary (e.g. query / think / report)
      naming what happens to its output next, derived from the node's own verdict —
      never set independently of it, so the two cannot disagree. Optional: only nodes
      whose output routes to more than one next step need to declare it.

  - id: TR-AGT-002
    title: Deterministic operations use scripts not agents
    status: active
    section: "Agent Spawning"
    text: >
      Operations that require repeatable results should be implemented in scripts,
      code, queries, tests, or validators. Agents may call those deterministic
      tools, but should not replace them.

  - id: TR-AGT-004
    title: Agent trigger type classified at design time
    status: active
    section: "Agent Spawning"
    text: >
      Each agent invocation is classified as event-driven, scheduled, or user-initiated.
      Undocumented triggers are non-compliant unmanaged side effects.

  - id: TR-AGT-005
    title: Behavioral mode declaration
    status: active
    section: "Agent Spawning"
    text: >
      A behavioral mode is a named, trigger-activated instruction set that changes how
      an agent approaches a task, orthogonal to process-intensity gate strictness. Each
      mode declares a trigger, activated behavior, exit condition, and precedence
      (safety and process-intensity rules always win on conflict).

  - id: TR-AGT-006
    title: Deterministic post-processing over agentic bookkeeping
    status: active
    section: "Agent Spawning"
    text: >
      Agents author content; deterministic code owns structure, indexes, metadata
      repair, and link integrity. Do not expose bookkeeping that a script can perform
      reliably as an agent tool the agent may forget to call. Reference shape: a single
      organize pass (heal → index sync → link check) shared by hook, schedule, CLI,
      and a read-only health tool.

  - id: TR-AGT-007
    title: Self-healing metadata with enrichment marker
    status: active
    section: "Agent Spawning"
    text: >
      When validating persistent records (e.g. note frontmatter), repair missing
      required fields deterministically and flag inventions with an explicit
      enrichment marker (e.g. `*_generated: true`) rather than rejecting the record.
      Remapping a legacy key to its current name is not an invention and must not set
      the marker. Later agent or human enrichment can target flagged records.

  - id: TR-AGT-008
    title: Split agent capabilities by determinism
    status: active
    section: "Agent Spawning"
    text: >
      Classify each capability into one of four tiers before choosing an agent surface:
      (1) invisible/automatic — no agent surface; (2) agent-invocable tools — genuine
      judgment calls only; (3) a thin policy skill — instructions for using existing
      tools well; (4) a CLI that works with no agent present. Deciding test, verbatim:
      if the agent can forget to run it, it isn't deterministic bookkeeping — it's
      another judgment call.

  - id: TR-AGT-009
    title: Event hooks carry cheap work; schedules carry expensive LLM work
    status: active
    section: "Agent Spawning"
    text: >
      Fire cheap deterministic work from event hooks (post-write heal, index sync).
      Batch expensive LLM work on a schedule so cost is a function of time, not event
      volume — N LLM calls in one sitting leave N−1 immediately superseded. Do not
      attach an LLM call to every ingest or edit event by default.

  - id: TR-AGT-010
    title: Tool contracts are frozen, versioned, and generated from live code
    status: active
    section: "Agent Spawning"
    text: >
      A tool surface an agent depends on (an MCP tool set, an internal API a pipeline
      calls) declares field names and semantics as additive-forever once shipped:
      existing fields are never removed, renamed, or re-typed, and new fields are
      always optional. Every structured response — success and error alike — carries
      a protocol_version that starts at 1 and increments only on a breaking change.
      The documentation describing the surface (a table of operations, their
      annotations, their status) is generated from the same live definitions the code
      registers, not maintained by hand as a separate artifact; a test compares the
      generated form against what is committed and fails on drift, and a second check
      compares the declared contract against what the registration code actually sets,
      so a hand-maintained doc and a hand-maintained declaration cannot silently
      diverge from each other or from the code. An error response missing
      protocol_version, or a documented annotation the registration code does not
      actually set, is a defect, not a documentation gap.

  - id: TR-AGT-011
    title: Hybrid deterministic-then-agentic classification tags its own stage
    status: active
    section: "Agent Spawning"
    text: >
      When a classification or triage pipeline runs multiple stages of increasing
      cost and judgment — a deterministic keyword/rule stage, an optional trained-model
      stage, an LLM stage — each result carries which stage produced it, from a closed
      vocabulary declared once and imported everywhere results are constructed.
      Deterministic stages run first and may short-circuit a later, more expensive
      stage entirely. A pipeline that has not yet built every stage the vocabulary
      names must say so rather than fabricate a stage's output to complete the shape —
      the tag records what actually ran, not what the architecture eventually intends.
      Reinforces TR-AGT-002 (deterministic checks before agent judgment) and TR-SEC-012
      (reject, never coerce, a model's output) at the level of pipeline architecture
      rather than a single field. A later stage overriding an earlier one on higher
      confidence is a natural extension of this pattern but is not itself required
      here — export that as its own practice once a real override path exists to
      evidence it, rather than asserting it as already covered by this ID.

  - id: TR-AGT-012
    title: Named agent authority manifests fail closed
    status: active
    section: "Agent Spawning"
    text: >
      When configuration declares a named agent endpoint, that endpoint shall declare
      an explicit non-empty tools allowlist. Configuration loading shall fail before
      endpoint startup when the allowlist is omitted or empty; an omitted or empty
      list shall never inherit the complete registered tool set. A deliberately
      unnamed legacy endpoint is a distinct compatibility mode and shall be selected
      only by omitting the named-agent manifest entirely. Each named endpoint shall
      register only its allowlisted tools and shall reject an allowlisted tool absent
      from the live registry. Endpoint identities shall match the transport's safe
      route grammar before use in a path, key, or authorization lookup. This
      requirement controls standing tool authority, not
      per-item data approval; a model-supplied tool argument is not owner
      authorization evidence.

  - id: TR-TEST-005
    title: LLM eval files co-located with agent modules
    status: active
    section: "Testing"
    text: >
      Each agent module that makes LLM calls should have a co-located
      tests/evals/test_<agent>_eval.py file with golden cases, a score function,
      and an LLM_EVAL=true guard so evals do not run in the standard unit suite.
      See templates/llm-eval.md.

  - id: TR-TEST-006
    title: Post-write state change verification for agent actions
    status: active
    section: "Testing"
    text: >
      Agent actions that produce external side effects, such as writing files,
      updating records, sending messages, or calling tools, should be followed by
      a deterministic verification step that confirms the observable state
      changed as expected.

  - id: TR-TEST-007
    title: Agent security-property claims verified against ground truth, not self-report
    status: active
    section: "Testing"
    text: >
      A claim about an agent's own behavior — obtained only by asking the agent
      in conversation ("do you have tool X," "do you remember Y") — is not
      verification evidence for a security-relevant property: isolation between
      agents, a permission boundary, or memory/session scoping. An agent's
      self-report can produce a false pass when the question is answered by the
      wrong backend, a stale cache, or the agent's own (possibly incorrect)
      belief about its state, none of which is the property actually under
      test. Verify instead against the system's own ground truth — the target
      API's own list/read endpoint, a database row, a server log line —
      independent of what the agent under test reports. This is a
      security-property-specific instance of the general "verify before
      referencing" discipline (TR-GOV-001's single-source-of-truth cousin,
      applied to runtime claims rather than static code symbols): the
      authoritative source for whether an isolation boundary holds is the
      boundary's own enforcement point, never the agent's narration of it.

  - id: TR-GOV-001
    title: Config/model-string consistency check
    status: active
    section: "Development Conventions"
    text: >
      scripts/check-config-consistency.py detects retired model strings and drift
      within an app. Run before merge; CI integration recommended.

  - id: TR-GOV-002
    title: Deferred-work tagging convention
    status: active
    section: "Development Conventions"
    text: >
      Tag deferred work as TECH-DEBT or POC-EXCEPTION. scripts/debt-report.py
      compiles tags for maturity checklist reconciliation.

  - id: TR-GOV-003
    title: Process intensity mode declared per work session
    status: active
    section: "Development Conventions"
    text: >
      Work sessions may declare a process intensity mode, such as full or
      poc-fast. Security and agent safety requirements still apply regardless of
      mode; deferred gates must be recorded as structured debt or POC exceptions.

  - id: TR-GOV-004
    title: Per-app maturity checklist
    status: active
    section: "App Starter Template"
    text: >
      Each app maintains docs/maturity-checklist.md mapping governance mechanisms
      to ISO 42001-inspired control objectives.

  - id: TR-GOV-005
    title: AI system impact assessment
    status: active
    section: "Architecture Decision Records"
    text: >
      ADRs that change AI behavior toward people require templates/ai-impact-assessment.md.

  - id: TR-GOV-006
    title: Recurring governance review cadence
    status: active
    section: "Documentation"
    text: >
      Governance reviewed at tier graduations and at minimum every 90 days using
      templates/governance-review.md.

  - id: TR-PUB-001
    title: Curated public release scope
    status: active
    section: "Public Release"
    text: >
      Public releases include only approved standards artifacts, examples,
      templates, scripts, and repository metadata. Unrelated application source
      code and local runtime artifacts are out of scope.

  - id: TR-PUB-002
    title: Public release secret and artifact denylist
    status: active
    section: "Public Release"
    text: >
      Before publish, scripts/public-export-check.py validates required public docs,
      forbidden artifact paths, secret-like patterns, and Python script syntax.
      The release-check CI workflow additionally runs detect-secrets on the staging
      tree (templates/ and examples/ excluded to avoid placeholder false positives).

  - id: TR-PUB-003
    title: License and attribution review
    status: active
    section: "Public Release"
    text: >
      Each release requires LICENSE and ATTRIBUTIONS.md. Maintainers should
      complete a license and attribution review before publishing.

  - id: TR-PUB-004
    title: Public support scope and issue triage
    status: active
    section: "Public Release"
    text: >
      CONTRIBUTING.md, SECURITY.md, issue templates, and label policy define support
      boundaries and triage for low-effort AI-generated reports.

  - id: TR-PUB-005
    title: Roadmap and changelog cadence
    status: active
    section: "Public Release"
    text: >
      Public repo maintains ROADMAP.md and CHANGELOG.md with periodic curated releases,
      not continuous private-repo mirroring.

  - id: TR-PUB-006
    title: Agent persona drift check between private and public trees
    status: active
    section: "Public Release"
    text: >
      A drift check compares the last-changed time of each private operating persona
      against its public-standards rewrite and fails if the private side changed more
      recently. This does not auto-sync content — the public rewrite stays hand-curated
      — it only forces a human reconciliation decision instead of silent drift.
